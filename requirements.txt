openpyxl==3.1.5
click>=8.1
# tomli is the TOML parser backport for Python < 3.11.
# Python 3.11+ ships tomllib in the stdlib; tomli is not needed there
# but installing it does no harm.
tomli>=2.0; python_version < "3.11"
